name: Commit

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  tests:
    uses: ./.github/workflows/tests.yml

  publish-nuget:
    needs: tests
    uses: ./.github/workflows/publish-nuget.yml
    with:
      dotnet-version: latest
    secrets: inherit
